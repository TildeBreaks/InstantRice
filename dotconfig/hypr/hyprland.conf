# Hyprland configuration file

# Source pywal colors
source = ~/.config/hypr/colors.conf

# Load plugins
plugin = /usr/lib/libhyprscroll.so

# Autostart applications
exec-once = quickshell --config ~/.config/quickshell/shell.qml &

# Keybindings
bind = SUPER, Return, exec, kitty
